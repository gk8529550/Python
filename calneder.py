# import module
import calendar

yy = int(input("enter the value of year which is want to you print on screen and show"))
mm=int(input("enter the value of month which is want to you print on screen"))

# display the calendar
print(calendar.month(yy, mm))
